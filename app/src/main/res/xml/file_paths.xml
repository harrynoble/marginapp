<?xml version="1.0" encoding="utf-8"?>
<!-- Only the export folder is shared, and only when the user chooses to share an export. -->
<paths>
    <cache-path name="exports" path="exports/" />
</paths>
